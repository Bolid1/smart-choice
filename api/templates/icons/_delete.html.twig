<button
        class="btn btn-link p-0"
        type="button"
        data-toggle="modal"
        data-target="#{{ modal }}"
        title="{{ title|default('delete' | trans) }}"
>
    {% include 'icons/_trash.svg.twig' %}
</button>
