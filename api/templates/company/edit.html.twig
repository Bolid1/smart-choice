{% extends 'base.html.twig' %}

{% block title %}Edit Company{% endblock %}

{% block main %}
    <main class="container">
        {{ include('company/_form.html.twig') }}
        {% if is_granted('delete', company) %}
            {{ include('company/_delete_form.html.twig') }}
        {% elseif company.userAdmin(app.user) %}
            {% trans %}You can delete a company when only one user remains in it.{% endtrans %}
        {% endif %}
    </main>
{% endblock %}
